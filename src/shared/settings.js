'use strict';

const CST = require('./constants');

module.exports = {
  /* release options:
    MARKETING_LAUNCH,
    PHASE1_LAUNCH,
    UNRESTRICTED */
  // release: CST.MARKETING_LAUNCH
  release: CST.UNRESTRICTED
}
